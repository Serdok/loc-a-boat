import { ToggleNavbarDirective } from './toggle-navbar.directive';

describe('ToggleNavbarDirective', () => {
  it('should create an instance', () => {
    // const directive = new ToggleNavbarDirective();
    // expect(directive).toBeTruthy();
  });
});
