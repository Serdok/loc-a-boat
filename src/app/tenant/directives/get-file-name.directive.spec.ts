import { GetFileNameDirective } from './get-file-name.directive';

describe('GetFileNameDirective', () => {
  it('should create an instance', () => {
    const directive = new GetFileNameDirective();
    expect(directive).toBeTruthy();
  });
});
